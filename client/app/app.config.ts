// app.config.ts
export default defineAppConfig({
	ui: {
		colors: {
			primary: "palette-navy-blue",
            secondary: "palette-red"
		},
        formField: {
            defaultVariants: {
                size: "xl"
            }
        }
	},
});
